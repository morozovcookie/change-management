operationId: getIncidentById

summary: Retrieve a single incident by id

description: |
  To retrieve information about specific incident, you should send a GET 
  request to `/api/v1/im/` with specified incident unique identifier.

tags:
  - IM

parameters:
  - $ref: ../../parameters/_index.yml#/incidentId

responses:
  200:
    $ref: responses/get_incident.yml

  400:
    $ref: ../../responses/not_found.yml
  
  500:
    $ref: ../../responses/internal.yml
