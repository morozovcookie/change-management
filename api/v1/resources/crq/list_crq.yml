operationId: listCRQ

summary: Get list of change requests

description: |
  To list all of the change requests, send a GET request to `/api/v1/crq`.

  The result will be a JSON object with a `data` key. This will be set to an 
  array of change requests, each of which will contain the change request 
  attributes.

tags:
  - CRQ

parameters:
  - $ref: ../../parameters/_index.yml#/start
  - $ref: ../../parameters/_index.yml#/limit

responses:
  200:
    $ref: responses/list_crq.yml

  400:
    $ref: ../../responses/_index.yml#/invalid

  500:
    $ref: ../../responses/_index.yml#/internal
